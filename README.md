# Data layout

Put annual CSV inputs in `processed/`, ONI data in `external/`, IMD annual CSVs in `processed/imd_temperature_csv/`, and rainfall NetCDF files in `raw/` or `processed/`. Large or restricted source data are intentionally not bundled.
